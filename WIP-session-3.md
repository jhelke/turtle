# WIP Session 3

## Code Gap

The managed mining loop can now handle one or more enabled turtle docks, and the
controller can service dock fuel/output chests. The remaining Session 3 gap is
indexed storage visibility.

Needed next:

- scan configured modem-connected storage chests
- record item name, count, peripheral name, and slot
- persist or print a simple item index
- add a small command to report/search stored item counts and locations

Output transfer exists. Searchable inventory indexing does not yet.
